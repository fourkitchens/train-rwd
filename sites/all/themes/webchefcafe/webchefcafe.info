name        = Web Chef Café
description = A basic responsive Drupal theme for training and demo purposes. Built with ❤ by Four Kitchens.
core        = 7.x
engine      = phptemplate

; CSS
stylessheets[all][] = css/h5bp.css
stylessheets[all][] = css/styles.css
stylessheets[print][] = css/print.css

;
; Alternate method for defining stylesheets with media queries
; - Use this if you want to split each media query into its own styleesheet
; - Creates extra aggregation files.
;
; stylesheets[(only screen and min-width: 320px)][] = fake/path/to.css
;

; JS


;
; Built by Four Kitchens
; No rights reserved. 
;
